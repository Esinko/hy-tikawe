Developer's Notes to Self:
- Sessions need to timeout
- Make sure database connections are always closed
- Require new password logic (forced reset too)
- Look into using named params to optimize large queries
- Feature posts and comments from user in profile
